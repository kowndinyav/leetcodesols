Clone graph

Intuition:
- BFS traversal
- V1 approach: have two passes - pass1, just create cloned nodes. pass2, update neighbors
- V2 approach: we could write the code not to have two passes. just a matter of thinking