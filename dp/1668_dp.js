var maxRepeating = function(sequence, word) {

    // Dimension - i - represents max k repetitions ending at i_th index

    // Base
    // find first i where i-m..i is WORD

    // Recurrence/Definition
    //  dp[i] = 1 + dp[i-m] -> if i-m+1..i is WORD
    
    // Return: max(dp)

}
